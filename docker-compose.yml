services:
  postgres:
    image: postgres:16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: hilite
      POSTGRES_PASSWORD: hilite
      POSTGRES_DB: hilite_erp
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
